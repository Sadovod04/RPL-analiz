"""Streamlit dashboard (M6).

Interactive ranking of current prospects (birth year >= scoring cutoff): filter
by features, inspect a player card with the SHAP breakdown of why the model
ranks them where it does.

Run: uv run --extra app streamlit run app/streamlit_dashboard.py
Status: skeleton — implemented in M6.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("M6")


if __name__ == "__main__":
    main()
